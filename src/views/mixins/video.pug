mixin video(video)
  a.videos__video(href=`/videos/${video.id}`)
    div
      h4= video.title
      p= video.description
      ul
        each hashtag in video.hashtags
          li= hashtag
      small= video.createdAt
